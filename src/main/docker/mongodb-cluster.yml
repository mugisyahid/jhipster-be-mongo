version: '2'
services:
    jbe-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb jbe-mongodb-config
    jbe-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    jbe-mongodb-config:
        image: mongo:4.0.2
        container_name: jbe-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
